{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "vue-tsc --noEmit --skipLibCheck -p tsconfig.json --composite false"
  },
  "devDependencies": {
    "@nuxt/devtools": "^1.0.8",
    "nuxt": "3.10.3",
    "nuxt-security": "^1.2.2"
  }
}
